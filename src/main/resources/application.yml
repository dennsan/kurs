
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/security
    username: postgres
    password: postgres


  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
logging:
  level:
    web: trace
    sql: error